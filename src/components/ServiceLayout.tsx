
type ServiceLayoutProps = {
    id: number
    name: string
    price: number
    city: string
    category: string
    imgUrl: string
  }

   export function ServiceLayout({ id, name, price, city, category, imgUrl }: ServiceLayoutProps) {
        return <li></li>
   }

